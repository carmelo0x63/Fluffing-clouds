---

- name: "Setup of the remote host, 20230406"
  hosts: all
  become: yes

  tasks:
  - name: "Upgrading all APT packages"
    apt:
      name: "*" 
      state: latest
      update_cache: yes 

  - name: "Installing missing packages"
    ansible.builtin.apt:
      name:
        - bind9-dnsutils

